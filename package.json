{
  "name": "typescript_nodejs_nodemon_template",
  "version": "1.0.0",
  "description": "Getting started with Typescript & nodeJs with nodemon for restarting the node application when file changes in the directory are detected.",
  "main": "index.js",
  "scripts": {
    "build": " rimraf ./build && tsc",
    "dev": "nodemon",
    "start": "npm run build && node ./build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vishal Talukar",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.8.10",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}